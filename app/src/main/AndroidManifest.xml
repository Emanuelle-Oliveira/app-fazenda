<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFazenda"
        tools:targetApi="31">
        <activity
            android:name=".ui.updateFarmActivity.UpdateFarmActivity"
            android:exported="false"
            android:label="@string/title_activity_update_farm"
            android:theme="@style/Theme.AppFazenda" />
        <activity
            android:name=".ui.createFarmActivity.CreateFarmActivity"
            android:exported="false"
            android:label="@string/title_activity_create_farm"
            android:theme="@style/Theme.AppFazenda" />
        <activity
            android:name=".ui.mainActivity.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppFazenda">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>